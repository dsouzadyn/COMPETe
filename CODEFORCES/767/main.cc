/**
 * @author      : Dylan Dsouza (dsouzadyn@gmail.com)
 * @created     : Friday May 11, 2018 21:28:08 IST
 */

#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    bool marked[100005] = {false};
    int n;
    cin >> n;
    int arr[100005] = {0};
    for(int i = 0; i <= n; i++) cin >> arr[i];
    for(int i = 1; i <= n; i++)

    return 0; 
}

